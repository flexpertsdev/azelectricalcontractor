<path>src/components/icons/IconShieldCheck.astro</path>
<content>
---
import type { HTMLAttributes } from 'astro/types';

/**
 * A shield icon with a checkmark, typically used to represent security,
 * verification, or trust (e.g., "Licensed, Bonded & Insured"). This component
 * renders a solid-style SVG that can be styled with Tailwind CSS classes.
 *
 * It uses `fill="currentColor"` so its color can be controlled by
 * Tailwind's text color utilities (e.g., `text-primary`, `text-secondary`).
 *
 * @example
 * <!-- Renders a blue icon with a height and width of 6 units -->
 * <IconShieldCheck class="h-6 w-6 text-primary" />
 */
export interface Props extends HTMLAttributes<'svg'> {
  /** Additional CSS classes to apply to the SVG element. */
  class?: string;
}

const { class: className, ...attrs } = Astro.props;
---

<svg
  xmlns="http://www.w3.org/2000/svg"
  viewBox="0 0 24 24"
  fill="currentColor"
  class:list={['h-6 w-6', className]}
  role="img"
  aria-hidden="true"
  focusable="false"
  {...attrs}
>
  <title>Shield Check Icon</title>
  <path
    fill-rule="evenodd"
    d="M12.516 2.17a.75.75 0 00-1.032 0 11.209 11.209 0 01-7.877 3.08.75.75 0 00-.722.515A12.001 12.001 0 002.25 9.75c0 5.942 4.064 10.933 9.563 12.348a.749.749 0 00.374 0c5.499-1.415 9.563-6.406 9.563-12.348 0-1.39-.223-2.73-.635-3.985a.75.75 0 00-.722-.516l-.143.001c-2.996 0-5.717-1.17-7.734-3.08zm3.094 8.016a.75.75 0 10-1.22-.872l-3.236 4.53L9.53 12.22a.75.75 0 00-1.06 1.06l2.25 2.25a.75.75 0 001.14-.094l3.75-5.25z"
    clip-rule="evenodd"></path>
</svg>
</content>